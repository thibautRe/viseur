import { styled } from '../stitches.config'

export const Typography = {
  H2: styled('h2', {
    fontSize: 15,
    fontWeight: 500,
    lineHeight: '15px',
  }),
  Label: styled('span', {
    fontSize: 10,
    fontWeight: 600,
    lineHeight: '12px',
    textTransform: 'uppercase',
  }),
}
